# Gateway Optimization TODO Round 13

- [x] 为 `concurrencyRecommendation` 增加稳定 reader 与类型
- [x] 为 `listenConfig` 增加稳定 reader 与类型
- [x] 让 `service-client` 与 `app-client` 使用统一 reader 返回强类型对象
- [x] 补运行时测试并完成 `pnpm test:runtime`
- [x] 完成 `pnpm build:desktop` 并提交本轮变更
