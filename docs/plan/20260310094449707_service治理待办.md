# Service 治理待办

## 下一阶段
- [x] 收敛 `crates/service/src/gateway/observability/http_bridge/stream_readers/*` 的 `use super::*`，改成显式依赖导入。
- [x] 继续细化 `crates/service/src/gateway/protocol_adapter/response_conversion/*`，把事件聚合、SSE 桥接、JSON 还原的可见性边界收稳。
- [x] 清理 `crates/service/src/app_settings/env_overrides/*` 中剩余的跨层访问，补一层明确门面。
- [x] 评估并开始收短 `crates/service/src/lib.rs` 的根级 `#[path]` 长清单，已完成 `usage`、`auth`、`requestlog`、`account`、`apikey` 子域门面化。
- [ ] 继续收 `crates/service/src/lib.rs` 剩余根层模块，优先评估 `requestlog` 之外的 `bootstrap`、`rpc_dispatch`、`storage` 相关分组。
- [ ] 清理 `gateway/protocol_adapter/request_mapping/*` 与相关测试中的 wildcard 导入。

## 验收口径
- [x] 每完成一个子域后重新执行 `cargo test -p codexmanager-service --no-run`。
- [x] 网关相关子域完成后执行 `cargo test -p codexmanager-service --test gateway_logs -- --nocapture`。
- [x] 设置相关子域完成后执行 `cargo test -p codexmanager-service --test app_settings -- --nocapture`。
- [x] 协议适配子域完成后执行 `cargo test -p codexmanager-service protocol_adapter -- --nocapture`。
- [x] 使用目录门面替代临时 `#[path]` 拼装时，根文件只保留模块声明与导出，不再承载业务实现。
