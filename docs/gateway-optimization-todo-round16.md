# Gateway Optimization TODO Round 16

- [x] 为 account import/export 与 secret 读取新增稳定 reader
- [x] 让 `account-client` 复用共享 reader 而不是直接信任 RPC 返回结构
- [x] 补运行时测试并完成 `pnpm test:runtime`
- [x] 完成 `pnpm build:desktop` 并提交本轮变更
